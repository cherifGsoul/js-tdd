{
  "name": "quotation",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.3"
  },
  "scripts": {
    "test:micro": "mocha --recursive tests/micro",
    "test:contract": "mocha --recursive tests/contract",
    "test:acceptance": "mocha --recursive tests/acceptance",
    "test": "mocha --recursive tests"
  }
}
